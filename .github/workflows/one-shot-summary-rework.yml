name: One-shot timeline summary rework

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-shot-summary-rework.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply summary patch
        run: python3 .github/patch-summary-rework.py
      - name: Validate inline JavaScript
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          text=Path('index.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',text,re.S|re.I)
          if not scripts:
              raise SystemExit('No inline scripts found')
          Path('/tmp/charming-inline.js').write_text('\n;\n'.join(scripts),encoding='utf-8')
          PY
          node --check /tmp/charming-inline.js
      - name: Commit patch and clean temporary files
        run: |
          rm .github/workflows/one-shot-summary-rework.yml .github/patch-summary-rework.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html .github/workflows/one-shot-summary-rework.yml .github/patch-summary-rework.py
          git commit -m 'Rework timeline summary metrics'
          git pull --rebase origin main
          git push origin HEAD:main
