name: Increase timeline season day size

on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch_timeline_season_day_size_v2.py'
      - '.github/workflows/apply-timeline-season-day-size-v2.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply larger season day label
        run: python scripts/patch_timeline_season_day_size_v2.py
      - name: Verify patch
        run: |
          grep -F 'font-size:11px' index.html
          grep -F '@media(max-width:620px){.dayMarker .seasonDayLabel{font-size:10px}}' index.html
      - name: Commit generated page
        run: |
          if git diff --quiet -- index.html; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Increase timeline season day label size'
          git push
