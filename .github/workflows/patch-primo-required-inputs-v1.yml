name: Patch finish-early half-day v1

on:
  push:
    paths:
      - .github/workflows/patch-primo-required-inputs-v1.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply finish-early half-day patch
        run: python3 scripts/patch_finish_early_half_days_v1.py
      - name: Commit finish-early half-day control
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+Ekstren@users.noreply.github.com'
          git add index.html assets/runtime.js assets/site.css
          git commit -m 'Allow half-day finish-early intervals'
          git push origin HEAD:main
