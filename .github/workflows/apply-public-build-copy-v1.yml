name: Apply public build copy cleanup

on:
  push:
    branches: [main]
    paths:
      - '.github/public-build-copy.trigger'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply build copy cleanup
        run: python .github/patch-public-build-copy-v1.py
      - name: Validate diff
        run: |
          git diff --check
          grep -q "Frigid Aura','Frigid Glint','Iron Fortress','Oath of Vigil" .github/build-fantomons-inject.html
          ! grep -q "Frigid Aura + Frigid Glint belong to Guardian DPS/Water mode" .github/build-fantomons-inject.html
          ! grep -q "synthesis" .github/build-fantomons-inject.html
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/build-fantomons-inject.html .github/build-maintenance.md scripts .github/*.py
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Clean up player-facing build copy and Guardian boss support"
          git push origin HEAD:main
